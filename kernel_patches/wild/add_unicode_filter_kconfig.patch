diff --git a/kernel/Kconfig b/kernel/Kconfig
--- a/kernel/Kconfig
+++ b/kernel/Kconfig
@@ -95,6 +95,18 @@ config KSU_SUSFS_SUS_MAP
         - It may not be able to evade detections by apps that implement a good injection detection.
         - Effective only on zygote spawned umounted user app process.

+config KSU_SUSFS_UNICODE_FILTER
+    bool "Enable Unicode bypass security filter"
+    depends on KSU_SUSFS
+    default y
+    help
+        - Block Unicode bypass attacks on Android's /sdcard/Android/data and /obb paths.
+        - Detects invisible/zero-width Unicode characters (U+200B, U+FEFF, etc.) used to
+          bypass Android scoped storage restrictions (CVE-2024-43093).
+        - Whitelist root (UID 0) and system (UID 1000) to prevent boot issues.
+        - Effective only on zygote spawned user app process.
+
 endmenu

 endmenu
